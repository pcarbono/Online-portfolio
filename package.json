{
  "homepage": "https://pcarbono.github.io/Online-porfolio",
  "name": "online-porfolio",
  "version": "1.0.0",
  "description": "pcarbono porfolio",
  "main": "index.html",
  "scripts": {
    "build": "if not exist build mkdir build && robocopy . build *.html *.css /s /njh /njs /ndl /nc /ns /np /xjd /xd build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "github.com/pcarbono/Online-porfolio"
  },
  "author": "Pedro Carbono",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^6.1.1"
  }
}
